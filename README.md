# human benchmark - aim trainer python bot

can get ~190 ms on aim trainer, i created this mostly for testing pyautogui and seeing how it worked.

## Features
- ~190 ms on human benchmark aim trainer
- uses pyautogui and target image to identify where the target is

## Setup
```bash
pip install -r requirements.txt
python main.py
